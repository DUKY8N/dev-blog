---
title: "제곱근을 이용한 소수 판별"
description: "약수의 성질을 이용해 제곱근까지만 확인하는 효율적인 소수 판별 방법."
date: "2026-08-08T13:00:00+09:00"
tags: [algorithm, math]
---

소수는 1보다 큰 자연수 중 약수가 1과 자기 자신뿐인 수다.  
코딩 테스트에서 어떤 수 $n$이 소수인지 확인해야 할 때, 2부터 $n - 1$까지 모두 나누어보는 방법을 먼저 떠올릴 수 있다.

하지만 약수는 항상 짝을 이룬다. 이 성질을 이용하면 $\sqrt{n}$까지만 확인해도 소수 여부를 판별할 수 있다.

---

## 왜 $\sqrt{n}$까지만 확인할까?

36의 약수는 다음처럼 짝을 이룬다.

```text
1 × 36
2 × 18
3 × 12
4 × 9
6 × 6
```

$\sqrt{36} = 6$보다 큰 약수는 이미 확인한 작은 약수의 짝이다.  
따라서 $\sqrt{n}$ 이하에서 약수를 찾지 못했다면, 그보다 큰 약수도 존재하지 않는다.

---

이 원리를 코드로 옮기면 다음과 같다.

```python
def is_prime(n):
    if n < 2:
        return False

    i = 2
    while i * i <= n:
        if n % i == 0:
            return False
        i += 1

    return True

assert is_prime(2)
assert not is_prime(1)
assert not is_prime(9)
```

0과 1은 소수가 아니므로 `n < 2`일 때는 바로 `False`를 반환한다.  
`i * i <= n` 조건으로 $\sqrt{n}$ 이하의 약수만 확인한다.

---

전체 수를 확인하는 대신 최대 $\sqrt{n}$번만 나누어보므로 시간 복잡도는 $O(\sqrt{n})$이다.
